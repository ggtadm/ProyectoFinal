@model List<ProyectoFinal.Models.Doctor>

@{
    ViewBag.Title = "Doctores";
    ViewBag.MostrarCerrarSesion = true;
    ViewBag.MostrarVolver = false;
}

<div class="container mt-5">
    
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0 text-primary fw-bold">
            🩺 Gestión de Doctores
        </h2>

        <div class="d-flex gap-2">
            <a href="#" class="btn btn-outline-secondary" onclick="return confirmarIrAlMenu();">
                ⬅ Volver al Menú
            </a>
            <a asp-action="Crear" class="btn btn-success">
                ➕ Nuevo Doctor
            </a>
        </div>
    </div>

    <div class="card shadow border-0 rounded">
        <div class="card-body">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover align-middle text-center">
                        <thead class="table-primary">
                            <tr>
                                <th>👨‍⚕️ Nombre del Doctor</th>
                                <th>⚙️ Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var doctor in Model)
                            {
                                <tr>
                                    <td>@doctor.Nombre</td>
                                    <td>
                                        <a asp-action="Editar" asp-route-id="@doctor.DoctorID" class="btn btn-warning btn-sm">
                                            ✏️ Editar
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="alert alert-warning text-center">
                    <span class="fw-semibold">🔔 No hay doctores registrados en el sistema.</span>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function confirmarIrAlMenu() {
            if (confirm("¿Estás seguro de que deseas volver al menú principal?")) {
                window.location.href = "/Home/Index";
                return false;
            }
            return false;
        }
    </script>
}



